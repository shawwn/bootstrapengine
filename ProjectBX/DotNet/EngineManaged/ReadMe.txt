The Bootstrap project is a .NET interface to the Bootstrap engine.  When a
.NET assembly adds a reference to Bootstrap.dll, they can then access the
"Bootstrap" namespace and begin interacting with the engine.
